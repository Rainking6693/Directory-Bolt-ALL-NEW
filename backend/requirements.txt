# Core dependencies
python-dotenv==1.0.0
pydantic==2.5.0

# Prefect orchestration
prefect==2.14.0
prefect-aws==0.4.0

# Database
supabase==2.8.1  # Updated to fix proxy compatibility issue
gotrue==2.8.1  # Required for supabase 2.8.1 compatibility
postgrest==0.17.0  # Pin postgrest to compatible version (supabase 2.8.1 requires >=0.17.0,<0.18.0)
psycopg2-binary==2.9.9
# Pin httpx to version compatible with supabase 2.8.1
httpx==0.24.1

# Queue
boto3==1.34.0  # AWS SQS
google-cloud-pubsub==2.18.0  # GCP Pub/Sub (optional)

# CrewAI
crewai==0.1.0
# CrewAI 0.1.0 requires langchain >=0.0.335,<0.0.336
langchain==0.0.335

# FastAPI for brain service
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Prefect dependencies (griffe is needed by Prefect 2.14.0)
griffe==0.40.0

# Browser automation
playwright==1.40.0

# AI services
anthropic==0.8.0
google-generativeai==0.3.0
beautifulsoup4==4.12.2  # HTML parsing for form mapping
lxml==4.9.3  # Fast XML/HTML parser for BeautifulSoup

# HTTP client (compatible with supabase 2.3.0 requirements)
requests==2.31.0

# Utilities
tenacity==8.2.3  # Retry logic

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
